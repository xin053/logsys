const state = {
  navigation: []
}

const getters = {
  navigation: state => state.navigation
}

const actions = {
}

const mutations = {
}

export default {
  state,
  getters,
  actions,
  mutations
}